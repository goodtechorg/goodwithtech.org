import React from "react"
import Layout from "../components/Layout"

const PageNotFoundPage = () => (<Layout title="Page not found">
    <div className="main-content">

      <header>
          <h1 className="index-title">Sorry, that page couldn't be found.</h1>
      </header>
    </div>
  </Layout>);

export default PageNotFoundPage;
